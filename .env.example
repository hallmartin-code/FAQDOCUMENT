# Copy to .env and fill in. .env is git-ignored — never commit a real key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (all have sane defaults)
# DECKPAGER_MODEL=claude-sonnet-4-6
# DECKPAGER_MIN_CONFIDENCE=0.6
# DECKPAGER_ENGINE=weasyprint
# DECKPAGER_CACHE_DIR=.deckpager-cache
# DECKPAGER_SOFFICE_PATH=C:\Program Files\LibreOffice\program\soffice.exe
