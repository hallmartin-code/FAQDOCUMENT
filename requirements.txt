# Railway builds from this file (NIXPACKS detects it). Local development uses
# `pip install -e ".[dev,web]"` against pyproject.toml instead. Keep these pins in step
# with the `dependencies` and `web` lists in pyproject.toml.
anthropic>=0.40
pydantic>=2
pydantic-settings>=2
typer>=0.12
rich>=13
pymupdf>=1.24
pdfplumber>=0.11
python-pptx>=1.0
reportlab>=4.1
python-dotenv>=1.0
pypdf>=4.2

# Web app only
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
